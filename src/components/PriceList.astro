<section class="price-cta">
  <div class="container">
    <div class="price-cta__card">
      <div class="price-cta__icon">🦷</div>
      <h2 class="price-cta__title">Стоимость — по записи</h2>
      <p class="price-cta__text">Финальная цена зависит от клинической картины, материалов и объёма работ. Позвоните — обсудим ваш случай и подберём оптимальный вариант.</p>
      <a href="tel:+79202537317" class="price-cta__btn">
        <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
        </svg>
        +7 (920) 253-73-17
      </a>
      <p class="price-cta__note">Бесплатная консультация</p>
    </div>
  </div>
</section>

<style>
.price-cta {
  padding: var(--space-4xl) 0;
  background: var(--color-bg-alt);
}

.price-cta__card {
  max-width: 600px;
  margin: 0 auto;
  text-align: center;
  background: var(--color-bg);
  border-radius: var(--radius-2xl);
  border: 1px solid var(--color-border-light);
  padding: var(--space-3xl);
  box-shadow: var(--shadow-lg);
}

.price-cta__icon {
  font-size: 3rem;
  margin-bottom: var(--space-lg);
}

.price-cta__title {
  font-size: var(--text-2xl);
  font-weight: 800;
  color: var(--color-text);
  margin-bottom: var(--space-md);
}

.price-cta__text {
  font-size: var(--text-base);
  color: var(--color-text-muted);
  line-height: var(--leading-relaxed);
  margin-bottom: var(--space-xl);
}

.price-cta__btn {
  display: inline-flex;
  align-items: center;
  gap: var(--space-sm);
  background: var(--color-primary);
  color: white;
  padding: var(--space-md) var(--space-2xl);
  border-radius: var(--radius-full);
  text-decoration: none;
  font-weight: 700;
  font-size: var(--text-xl);
  transition: all var(--transition-fast);
}

.price-cta__btn:hover {
  background: var(--color-primary-hover);
  box-shadow: var(--shadow-primary);
  transform: translateY(-2px);
  text-decoration: none;
}

.price-cta__note {
  font-size: var(--text-sm);
  color: var(--color-text-muted);
  margin-top: var(--space-md);
}

@media (max-width: 640px) {
  .price-cta { padding: var(--space-2xl) 0; }
  .price-cta__card { padding: var(--space-xl); }
  .price-cta__title { font-size: var(--text-xl); }
  .price-cta__btn { font-size: var(--text-lg); padding: var(--space-sm) var(--space-lg); }
}
</style>
